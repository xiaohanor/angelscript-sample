namespace DragonSword
{
	const FName HandSocket = n"RightAttach";
	const FName BackSocket = n"Backpack";

	const FRotator HandMeshRelativeRotation = FRotator(0, 85, 0);
	const FRotator BackMeshRelativeRotation = FRotator( 180, -7, -35);
	const FVector BackMeshRelativeLocation = FVector(0, 25, 20);
}